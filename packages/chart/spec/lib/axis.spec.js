import { describe, it, expect } from 'vitest'
import { axis, axisTicks, Axis } from '../../src/lib/axis.js'

describe('axis', () => {
	describe('axisTicks', () => {
		it('should create axis ticks', () => {})
	})
	// describe('axis', () => {})
	describe('Axis', () => {
		it('should create an axis object', () => {
			// const data = new Axis('x', {}, 0)
			// expect(data).toBeDefined()
		})
	})
})
