import { describe, it, expect } from 'vitest'
import {
	summarize,
	getUniques,
	fillMissing,
	convertToPhases,
	mirror,
	getScales,
	getLabels,
	getPaths,
	funnel
} from '../../src/lib/funnel.js'
describe('funnel', () => {
	describe('summarize', () => {
		it('should summarize data', () => {})
	})
	describe('getUniques', () => {
		it('should get unique values', () => {})
	})
	describe('fillMissing', () => {
		it('should fill missing data', () => {})
	})
	describe('convertToPhases', () => {
		it('should convert data to phases', () => {})
	})
	describe('mirror', () => {
		it('should mirror the data', () => {})
	})
	describe('getScales', () => {
		it('should create scales', () => {})
	})
	describe('getLabels', () => {
		it('should create labels', () => {})
	})
	describe('getPaths', () => {
		it('should create a path', () => {})
	})
	describe('funnel', () => {
		it('should create a funnel object', () => {})
	})
})
