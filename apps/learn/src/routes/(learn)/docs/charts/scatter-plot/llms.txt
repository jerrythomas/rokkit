# ScatterPlot

Data-driven SVG scatter plot for @rokkit/chart.

## Import

```svelte
import { ScatterPlot } from '@rokkit/chart'
```

## Usage

```svelte
<!-- Basic scatter plot -->
<ScatterPlot data={data} x="sessions" y="conversions" />

<!-- With color coding -->
<ScatterPlot data={data} x="sessions" y="conversions" color="channel" legend />

<!-- Without grid -->
<ScatterPlot data={data} x="sessions" y="conversions" grid={false} />
```

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| data | Object[] | [] | Chart data |
| x | string | — | X axis field (numeric) |
| y | string | — | Y axis field (numeric) |
| color | string | — | Color channel field |
| pattern | string | — | Pattern fill channel field |
| fill | string | — | Fill channel field |
| size | string | — | Size channel field |
| width | number | 600 | SVG width |
| height | number | 400 | SVG height |
| mode | 'light' \| 'dark' | 'light' | Color palette mode |
| grid | boolean | true | Show gridlines |
| legend | boolean | false | Show legend |

## Notes

- Uses ChartBrewer internally for reactive data binding
- Renders SVG circle elements for each data point
- Compatible with the declarative Chart wrapper via shared context key
