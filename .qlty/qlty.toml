# This file was automatically generated by `qlty init`.
# You can modify it to suit your needs.
# We recommend you to commit this file to your repository.
#
# This configuration is used by both Qlty CLI and Qlty Cloud.
#
#     Qlty CLI -- Code quality toolkit for developers
#     Qlty Cloud -- Fully automated Code Health Platform
#
# Try Qlty Cloud: https://qlty.sh
#
# For a guide to configuration, visit https://qlty.sh/d/config
# Or for a full reference, visit https://qlty.sh/d/qlty-toml
config_version = "0"

exclude_patterns = [
  "pnpm-lock.yaml",
  '**/app.d.ts',
  "**/fixtures/**/invalid.js",
  "**/fixtures/error/*.js",
  "*_min.*",
  "*-min.*",
  "*.min.*",
  "**/*.d.ts",
  "**/.yarn/**",
  "**/bower_components/**",
  "**/build/**",
  "**/cache/**",
  "**/config/**",
  "**/db/**",
  "**/deps/**",
  "**/dist/**",
  "**/extern/**",
  "**/external/**",
  "**/generated/**",
  "**/Godeps/**",
  "**/gradlew/**",
  "**/mvnw/**",
  "**/node_modules/**",
  "**/protos/**",
  "**/seed/**",
  "**/target/**",
  "**/testdata/**",
  "**/vendor/**",
  "**/assets/**",
]

test_patterns = [
  "**/test/**",
  "**/spec/**",
  "**/*.test.*",
  "**/*.spec.*",
  "**/*_test.*",
  "**/*_spec.*",
  "**/test_*.*",
  "**/spec_*.*",
]

[smells]
mode = "comment"

[[source]]
name = "default"
default = true

[[plugin]]
name = "actionlint"

[[plugin]]
name = "brakeman"

[[plugin]]
name = "checkov"

# eslint + prettier are run by the repo itself (`bun run lint` / `bun run format`,
# which pass `--config config/eslint.config.mjs` / `config/.prettierrc`). qlty's
# bundled copies can't discover the configs under config/ and error out (qlty's
# isolated tool dir can't resolve a re-export shim, and it ignores .prettierignore
# so it chokes on package.json). Disabled here to avoid redundant, broken runs.
[[plugin]]
name = "eslint"
version = "9.7.0"
package_file = "package.json"
package_filters = ["eslint", "prettier"]
mode = "disabled"

[[plugin]]
name = "golangci-lint"

[[plugin]]
name = "markdownlint"
version = "0.41.0"
# Linter only — the `fmt` formatter driver rewrites prose (strips spaces around
# inline code, reflows lists) and corrupts content; formatting is hand-managed.
drivers = ["lint"]

[[plugin]]
name = "osv-scanner"

[[plugin]]
name = "prettier"
package_file = "package.json"
package_filters = ["prettier"]
mode = "disabled"

[[plugin]]
name = "ripgrep"

[[plugin]]
name = "trivy"
drivers = ["config", "fs-vuln"]

[[plugin]]
name = "trufflehog"

[[plugin]]
name = "yamllint"
