// @ts-nocheck
// Map custom field names to the expected keys
const fields = {
	label: 'fileName',   // display text
	value: 'id',         // unique identifier
	status: 'state',     // upload state
	progress: 'pct',     // progress percentage
	size: 'bytes',       // file size
	type: 'mime'         // MIME type
}
