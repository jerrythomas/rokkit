# 001 — Tree

Removed @rokkit/ui Tree dependency — tasks are flat, Tree added complexity and was swallowing click events. Got this when building a tree with usage in another app.

http://localhost:5174/welcome/introduction.

- header is fine
- sub header seems to be missing an icon
- Hamburger icon for collapsing sidebar is missing
- The sidebar list (grouped) seems to have issues.
  - scroll up/down is not working
  - styles don't seem to match rokkit
  - is this the list component?
  - expand collapse works
  - can't make out the hover style at all except on group item
  - clicking doens't work because wrong snippet name was used. if incorrect snippet name is used the whole section is overridden. 
  - If i go with default render, the list is working as expected. however  current item is not highlighted. i tried adding value={$page.url.pathname} and fields { value:'slug' }, but it is causing errors.
  - When we use custom item, the hyperlink is lost (the wrapper should remain same button/a only content sould be included. but something else is being done. will check. The content was using wrong snippet. 
  - The List does not seem to be using the nested controller and navigator properly. There is too much code in the component. The controller is supposed to help navigate the list which is why we provide the wrapper object into the controller. The controller performs actions based on navigator events. Here the logic seems to be too convoluted. it is not being use dthe way these two were designed. 
  - functionality should have been
  -   We create controller by passing items, value, fields and options. value is expected to be one item of the items. now we can assume this to be a primitive in which case we match with the proxy.get('value'). list controller has properties that help identify focused keys, selected keys expanded keys etc, the ui is supposed to use these properties to appropriately change the state. When expand key is pressed using navigator, navigator calls the function expand on the controller, this changes the value of expandedkeys and that should reactively map on the list. Maybe you should rever to verstion tagged 1.0.0-next.100 and see how it was used. Please read through understand the pattern and lets work on simplifying List component.
