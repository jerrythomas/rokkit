{
	"name": "svelte-spice",
	"version": "1.0.0-next.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"prepublishOnly": "turbo run prepublishOnly",
		"build": "turbo run build",
		"test": "turbo run test:ci",
		"coverage": "turbo run coverage",
		"lint": "turbo run lint",
		"format": "turbo run format",
		"prepare": "husky install",
		"upgrade": "pnpm upgrade",
		"changeset": "changeset",
		"version-packages": "changeset version",
		"version:next": "turbo run build && changeset",
		"release:next": "turbo run build && changeset publish",
		"upgrade:all": "turbo run upgrade",
		"coverage:lcov": "pnpm coverage && ./merge-lcov.sh"
	},
	"author": "Jerry Thomas<me@jerrythomas.name",
	"license": "MIT",
	"workspaces": [
		"packages/core",
		"packages/input",
		"packages/form",
		"packages/markdown",
		"packages/chart",
		"packages/themes",
		"packages/layout",
		"adapters/supabase",
		"adapters/firebase"
	],
	"devDependencies": {
		"@changesets/cli": "^2.25.2",
		"eslint": "^7.32.0",
		"eslint-config-prettier": "^8.5.0",
		"husky": "^7.0.4",
		"lcov-result-merger": "^3.3.0",
		"lint-staged": "^11.2.6",
		"prettier": "^2.7.1",
		"turbo": "^1.5.5"
	},
	"lint-staged": {
		"*.{js,svelte}": "eslint --cache --fix",
		"*.{js,svelte,md,json}": "prettier --write"
	}
}
